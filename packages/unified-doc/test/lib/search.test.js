import { getNodes, serializeResult } from '~/unified-doc/lib/search';

describe('search', () => {
  describe(getNodes.name, () => {
    it('TODO: will be implemented', () => {});
  });

  describe(serializeResult.name, () => {
    it('TODO: will be implemented', () => {});
  });
});
